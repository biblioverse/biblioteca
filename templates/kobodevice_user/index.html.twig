{% extends 'base.html.twig' %}

{% block title %}Kobo index{% endblock %}

{% block body %}
    <table class="table table-bordered table-striped table-hover">
        <thead>
            <tr>
                <th>Name</th>
                <th>AccessKey</th>
                <th>{{'shelves'|trans}}</th>
            </tr>
        </thead>
        <tbody>
        {% for kobo in kobos %}
            <tr>
                <td>
                    {% if is_granted('EDIT', kobo) %}
                        <a href="{{ path('app_kobodevice_user_edit', {'id': kobo.id}) }}">
                    {% endif %}
                    {{ kobo.name }}
                    {% if is_granted('EDIT', kobo) %}
                        </a>
                    {% endif %}
                {% if kobo.model is not empty %}
                   <br /><small class="text-muted">{{ kobo.model }}</small>
                {% endif %}
                </td>
                <td><code>{{ kobo.accessKey }}</code></td>
                <td>
                    <ul>
                    {% for shelf in kobo.shelves %}
                        <li>{{ shelf.name }}</li>
                    {% endfor %}
                    </ul>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_kobodevice_user_new') }}" class="btn btn-primary">Create new</a>

    <a href="{{ path('app_kobodevice_user_logs') }}" class="btn btn-primary">Logs</a>


    {{ include('kobodevice_user/instructions.html.twig') }}
{% endblock %}
