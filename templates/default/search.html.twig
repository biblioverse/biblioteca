{% extends 'base.html.twig' %}

{% block body %}


    <div class="container px-8 mx-auto xl:px-5  max-w-screen-lg py-5 lg:py-8">
        <div>
            <form class="mx-auto text-center">
                <input type="text" value="{{ term }}" name="term" class="border rounded-lg p-3" />
                <input type="submit" value="Chercher" class="border rounded-lg p-3 bg-red-500 text-white">
            </form>
        </div>
        <div class="grid mt-10 gap-10 md:grid-cols-4">
            {% for article in articles %}
                {% include 'article/_teaser.html.twig' %}
            {% else %}
                {% if term!='' %}
                Aucun résultat pour "{{ term }}"
                {% endif %}
            {% endfor %}
        </div>
    </div>


{% endblock %}
