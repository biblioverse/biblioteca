<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('{{ "generic.are-you-sure-you-want-to-delete-this-item"|trans }}?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger">{{ "generic.delete"|trans }}</button>
</form>
